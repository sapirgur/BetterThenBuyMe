<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>עמוד צור קשר</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/contactUs.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/aboutUs.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= googleMapsApiKey %>" defer></script>
    <script src="/js/contactUs.js" defer></script>
    <style>
        .manager {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }
        .manager img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
        }
        .manager .description {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border-radius: 5px;
            flex-grow: 1;
            margin: 20px;
        }
        .left-img {
            order: 1;
        }
        .right-img {
            order: 3;
        }
        .description {
            order: 2;
            width: calc(100% - 140px); /* Adjusting the width to accommodate image */
        }
    </style>
</head>
<body>
    <!-- Header -->
    <%- include('partials/header') %>

    <!-- Main Content -->
    <div class="container mt-5">
        <h1 class="text-center">צור קשר</h1>
        <div class="mt-4">
            <% managers.forEach((manager, index) => { %>
                <div class="manager">
                    <% if (index % 2 === 0) { %>
                        <img src="<%= manager.imgpath %>" alt="Profile Image" class="left-img">
                    <% } %>
                    <div class="description">
                        <h3><%= manager.name %></h3>
                        <p><%= manager.description %></p>
                        <a href="mailto:<%= manager.email %>"><%= manager.email %></a>
                    </div>
                    <% if (index % 2 !== 0) { %>
                        <img src="<%= manager.imgpath %>" alt="Profile Image" class="right-img">
                    <% } %>
                </div>
            <% }) %>
        </div>
        <div class="row mt-5">
            <div class="col-12">
                <!-- Map Container -->
                <div id="map" style="height: 500px; width: 100%;"></div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.3/js/bootstrap.min.js"></script>

</body>
</html>
