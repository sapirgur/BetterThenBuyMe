<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/Public/css/testimonials.css"> <!-- Link to your new CSS file -->
    <title>Testimonials</title>
</head>
<body>
    <section class="testimonials bg-light py-5">
        <div class="container">
            <h2 class="text-center mb-4">מה הלקוחות שלנו אומרים</h2>
            <div class="row">
                <% topReviews.forEach(review => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body d-flex flex-column">
                                <p class="card-text text-center flex-grow-1">"<%= review.comment %>"</p>
                                <div class="d-flex justify-content-center align-items-center">
                                    <div class="stars">
                                        <% for (let i = 0; i < 5; i++) { %>
                                            <% if (i < review.rating) { %>
                                                <i class="fas fa-star text-warning fa-2x"></i>
                                            <% } else { %>
                                                <i class="far fa-star text-warning fa-2x"></i>
                                            <% } %>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </section>
</body>
</html>
